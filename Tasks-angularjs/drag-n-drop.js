// var element = document.getElementById(`${task.title}${task.id}`)
            // // // element.style.zIndex = 1000;

            // // let shiftX = $event.clientX - element.getBoundingClientRect().left;
            // // let shiftY = $event.clientY - element.getBoundingClientRect().top;

            // // element.style.position = 'absolute';
            // // element.style.zIndex = 1000;
            // // document.body.append(element);

            // // moveTo(element, $event.pageX, $event.pageY, shiftX, shiftY);
            // // element.style.left = $event.pageX - shiftX + 'px';
            // // element.style.top = $event.pageY - shiftY + 'px';
            
            // // console.log($event.pageX + ">>>>>>>>" + $event.pageY)

            // // var windowHeight = window.innerHeight
            // // var windowWidth= window.innerWidth

            // // var tasksSpace = document.getElementById("tasksSpace")

            // // var tasksSpaceWidth = tasksSpace.offsetWidth
            // // var tasksSpaceHeight = tasksSpace.offsetHeight


            // // console.log(tasksSpace.offsetHeight + ">>>>>>>>" + tasksSpace.offsetWidth)
            // // console.log("window size : { \n     X : " + windowWidth + ",\n      Y : " + windowHeight + ",\n}" )
            // var element = document.getElementById(`${task.title}${task.id}`)
            // element.style.position = "absolute";
            // element.style.left = $event.x - element.getBoundingClientRect().left +'px';
            // element.style.top = $event.y - element.getBoundingClientRect().top +'px';
            // // console.log(element.getBoundingClientRect().left + ">>>>>>>>" + element.getBoundingClientRect().top)
            // // console.log(`${task.title} ...mouse moving ========> X = ${$event.x}, Y = ${$event.y}`)